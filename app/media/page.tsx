import { Metadata } from "next"
import { PAGEMEDIA } from "../utils/constant"

export const generateMetadata = (): Metadata => {
    return {
        title: PAGEMEDIA
    }
}
export default function Media() {
    return <></>
}